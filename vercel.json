{
  "version": 2,
  "buildCommand": "cd client && npm install && npm install --save-dev tailwindcss postcss autoprefixer @types/node && npm run build",
  "outputDirectory": "client/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "env": {
    "VITE_API_URL": "https://barberbooker.onrender.com"
  }
}
